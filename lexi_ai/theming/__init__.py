"""Themed generation (Phase 2): restyle a done word's senses in a named voice.

The neutral entry stays canonical; themed definitions + fresh in-voice examples
overlay it (Phase 3). Generation is anchored to neutral sense FACTS (definition,
pos, guideword, tier) — never neutral examples, which are re-authored in-voice.
"""
